# pyproject.toml
[project]
name = "ski-game"
version = "1.0.0"
description = "A fun skiing game built with Pygame"
authors = [
    {name = "Your Name", email = "your.email@example.com"}
]
dependencies = [
    "pygame>=2.0.0"
]

[project.optional-dependencies]
dev = [
    "pytest",
    "black"
]
